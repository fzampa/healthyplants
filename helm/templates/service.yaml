apiVersion: v1
kind: Service
metadata:
  name: {{ include "healthy-plants.fullname" . }}
  labels:
    {{- include "healthy-plants.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "healthy-plants.selectorLabels" . | nindent 4 }}
