apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "healthy-plants.fullname" . }}-test-connection"
  labels:
    {{- include "healthy-plants.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "healthy-plants.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
